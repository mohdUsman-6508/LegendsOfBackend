<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>studentExist</title>
    <style>
      /* Style for studentExist.html */

      body {
        font-family: Arial, sans-serif;
        background-color: #f0f0f0;
        margin: 0;
        padding: 0;
      }

      .warn {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .warning {
        background-color: #ff7f50; /* Coral color */
        color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        text-align: center;
      }

      /* Responsive design */
      @media (max-width: 600px) {
        .warning {
          width: 80%;
        }
      }
    </style>
  </head>
  <body>
    <div class="warn">
      <div class="warning">Student Exist!</div>
    </div>
  </body>
</html>
